#include "types.h"

// Get mass of a circle based on radius and density
extern f32 s_circle_mass_f32(const f32 radius, const f32 density);
// Get mass of a circle based on radius and density
extern f64 s_circle_mass_f64(const f64 radius, const f64 density);
