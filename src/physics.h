#include "types.h"

// Get mass of a circle based on radius and density
extern f64 f_circle_mass_d(const f64 radius, const f64 density);
// Get mass of a circle based on radius and density
extern f32 f_circle_mass_f(const f32 radius, const f32 density);
